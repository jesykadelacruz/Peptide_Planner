--- a/index.html
+++ b/index.html
@@ -12,7 +12,7 @@
 <meta http-equiv="Pragma" content="no-cache">
 <meta http-equiv="Expires" content="0">
 <link rel="manifest" href="./manifest.webmanifest">
-<title>Peptide_Planner_v20261009.13</title>
+<title>Peptide_Planner_v20261109.01</title>
 <!-- v2026.0909.04: Production hardening only. Cloud writes are serialized so overlapping autosaves cannot race or clear a newer local edit; successful writes preserve the dirty state when another edit occurred in flight. Password-reset redirects resolve from the planner URL currently in use instead of a fixed Pages hostname. Treatment, dose, vial, schedule, role and layout behavior are unchanged. -->
 <!-- v2026.0909.03: Continuous and cycled treatments now share the same pending replacement-vial transition. A replacement vial stays pending until its first scheduled administration date; after the prior vial can no longer supply another full scheduled dose, Dose left in vial shows Open Vial instead of 0, and the replacement vial becomes current on its first-use date. All unrelated treatment, sync, auth, role and UI behavior is unchanged. -->
 <!-- v2026.0909.02: Cache/runtime synchronization release. Production treatment UI, vial logic, dose calculations, cross-device sync and role behavior are unchanged from v2026.0909.01; the service worker version is advanced to the same release so devices cannot remain on an older cached planner. -->
@@ -7391,7 +7391,7 @@
 "use strict";
 const STORAGE_KEY="peptideTreatmentPlanner_v4";
 const LEGACY_KEY="personalTreatmentTracker_v1";
-const APP_VERSION="Peptide_Planner_v20261009.13";
+const APP_VERSION="Peptide_Planner_v20261109.01";
 const CLOUD_SYNC_META_KEY="peptidePlannerCloudSync_v1";
 const CLOUD_KB_CACHE_KEY="peptidePlannerKbOverrides_v1";
 const SUPABASE_URL="https://srfuwxjwulvbalzoymcc.supabase.co";
@@ -7630,9 +7630,18 @@
     if(prevEnd){cfg.startDate=localDateISO(addDays(prevEnd,1));cfg.startAuto=true;}
   }
 }
+let plannerRenderCycleConfigCache=null;
+let plannerRenderCalculationCache=null;
+function plannerRenderCalculationBucket(plan){
+  if(!plannerRenderCalculationCache||!plan)return null;
+  let bucket=plannerRenderCalculationCache.get(plan);
+  if(!bucket){bucket=new Map();plannerRenderCalculationCache.set(plan,bucket);}
+  return bucket;
+}
 function ensurePlanCycleConfigs(plan){
   if(!plan)return [];
   if(plan.noCycle)return [];
+  if(plannerRenderCycleConfigCache?.has(plan))return plannerRenderCycleConfigCache.get(plan);
   const count=planCyclesPerYear(plan);
   let configs=(Array.isArray(plan.cycleConfigs)?plan.cycleConfigs:[]).filter(item=>item&&typeof item==="object").map(item=>clone(item));
   if(!configs.length)configs=[cycleConfigDefault(plan,1)];
@@ -7657,7 +7666,9 @@
   plan.cyclesPerYear=count;plan.fullCyclesPerYear=count;plan.repeatCycles=count;
   recalculateConfiguredCycleStarts(plan);
   syncLegacyPlanFromCycleConfigs(plan);
-  return plan.cycleConfigs.slice(0,count);
+  const resolved=plan.cycleConfigs.slice(0,count);
+  if(plannerRenderCycleConfigCache)plannerRenderCycleConfigCache.set(plan,resolved);
+  return resolved;
 }
 function cycleConfigForSequence(plan,sequence){
   if(!plan)return null;
@@ -8093,6 +8104,20 @@
 function plannerCanAccessAdmin(){
   return !!(cloudUser&&cloudAccess?.status==="active"&&["owner","admin"].includes(cloudAccess?.role||""));
 }
+function renderPrimaryTabContent(tabId){
+  const previousCycleConfigCache=plannerRenderCycleConfigCache,previousCalculationCache=plannerRenderCalculationCache;
+  plannerRenderCycleConfigCache=new WeakMap();
+  plannerRenderCalculationCache=new WeakMap();
+  try{
+    if(tabId==="dashboard")renderDashboard();
+    else if(tabId==="planner")renderSimplePlanner();
+    else if(tabId==="protocols")renderProtocolLibrary();
+    else if(tabId==="progress")renderTables();
+    else if(tabId==="guidelines")renderGuidelinePlanOptions();
+    else if(tabId==="admin")renderAdminOptions();
+    else if(tabId==="calendar")renderCalendar();
+  }finally{plannerRenderCycleConfigCache=previousCycleConfigCache;plannerRenderCalculationCache=previousCalculationCache;}
+}
 function activateTab(tabId){
   // v2026.0709.01: role changes only Admin Dashboard access. All planner tabs use the same path.
   if(tabId==="admin"&&!plannerCanAccessAdmin())tabId="dashboard";
@@ -8107,6 +8132,10 @@
   const button=[...document.querySelectorAll(".tab-btn")].find(item=>item.dataset.tab===navTab);
   document.querySelectorAll(".tab-btn").forEach(item=>item.classList.toggle("active",item===button));
   document.querySelectorAll(".tab").forEach(item=>item.classList.toggle("active",item.id===tabId));
+  renderPrimaryTabContent(tabId);
+  enhanceMobileTables();
+  enhanceDateInputs();
+  syncAllDateDisplays();
   if(button)button.scrollIntoView({behavior:"smooth",block:"nearest",inline:"center"});
   window.scrollTo({top:0,behavior:"smooth"});
   if(["dashboard","progress"].includes(tabId))requestAnimationFrame(drawCharts);
@@ -8330,9 +8359,13 @@
   if(!phase?.splitDose){
     return [{partKey:"",partNumber:1,partCount:1,doseMg:resolvedDose,units:resolvedUnits,time:plan.injectionTime||""}];
   }
-  const parts=simplePhaseSplitParts(phase),times=Array.isArray(phase.splitTimes)?phase.splitTimes:[],splitUnits=phaseSplitUnitValues(plan,phase,date,sequence);
+  const parts=simplePhaseSplitParts(phase),times=Array.isArray(phase.splitTimes)?phase.splitTimes:[],splitUnits=phaseSplitUnitValues(plan,phase,date,sequence),entryMode=inferPhaseDoseEntryMode(plan,phase,date,sequence);
+  let allocatedDose=0;
   return Array.from({length:parts},(_,index)=>{
-    const partUnits=num(splitUnits[index]),partDose=partUnits!=null?phaseDoseFromUnits(plan,partUnits,date,sequence):(resolvedDose!=null?round(Number(resolvedDose)/parts,6):null);
+    const partUnits=num(splitUnits[index]);
+    let partDose=null;
+    if(entryMode==="units"&&partUnits!=null)partDose=phaseDoseFromUnits(plan,partUnits,date,sequence);
+    else if(resolvedDose!=null){partDose=index===parts-1?round(Number(resolvedDose)-allocatedDose,6):round(Number(resolvedDose)/parts,6);allocatedDose=round(allocatedDose+partDose,6);}
     return {partKey:`split-${index+1}`,partNumber:index+1,partCount:parts,doseMg:partDose,units:partUnits,time:times[index]||""};
   });
 }
@@ -10928,7 +10961,7 @@
     const record=item.record||{};
     if(closed.has(String(record.status||"")))return false;
     const explicitFinish=String(record.finishDate||"").trim();
-    if(explicitFinish&&today>=explicitFinish)return false;
+    if(explicitFinish&&today>explicitFinish)return false;
     const scheduledFinish=String(scheduledByRecordId.get(record.id)?.finishDate||"").trim();
     if(!explicitFinish&&scheduledFinish&&today>scheduledFinish)return false;
     return true;
@@ -12660,7 +12693,7 @@
 
 function cycleVialScheduleStatus(openDate,finishDate,at=new Date()){
   const today=localDateISO(at);
-  if(finishDate&&today>=finishDate)return "Completed";
+  if(finishDate&&today>finishDate)return "Completed";
   if(openDate&&today>=openDate)return "Actively Using";
   return "Pending";
 }
@@ -12671,6 +12704,8 @@
   const unitsPerMl=num(plan?.unitsPerMl);
   if(!unitsPerMl||unitsPerMl<=0)return {valid:false,reason:"Enter syringe calibration"};
   const requestedCycle=isSeparateCycleStack(plan)?planCycleNumber(plan):Math.max(1,Math.floor(num(cycleNumber)||1));
+  const renderCache=plannerRenderCalculationBucket(plan),renderCacheKey=`cycle-dose-events:${requestedCycle}`;
+  if(renderCache?.has(renderCacheKey))return renderCache.get(renderCacheKey);
   const period=protocolPeriodForSequence(plan,requestedCycle);
   if(!period?.valid)return {valid:false,reason:requestedCycle>1?"Enter maintenance weeks":"Enter active weeks"};
   const usageSchedule={...plan};
@@ -12695,7 +12730,9 @@
   }
   if(!events.length)return {valid:false,reason:"Complete the cycle schedule and dose/units"};
   const prep=cycleVialPrepForSequence(plan,requestedCycle),unitsPerVial=prep?.finalMl>0?round(prep.finalMl*unitsPerMl,6):0;
-  return {valid:true,cycleNumber:requestedCycle,period,cycleStart,activeEnd,events,unitsPerVial};
+  const result={valid:true,cycleNumber:requestedCycle,period,cycleStart,activeEnd,events,unitsPerVial};
+  if(renderCache)renderCache.set(renderCacheKey,result);
+  return result;
 }
 function cycleVialScheduleRecordPool(plan,cycleUsage,cycleNumber){
   const period=protocolPeriodForSequence(plan,cycleNumber);
@@ -12742,13 +12779,8 @@
   return count;
 }
 function cycleFullDoseCountForDisplay(events,startCursor,openDate,unitsPerVial){
-  const open=parseISO(openDate);
-  if(!open||!unitsPerVial||unitsPerVial<=0||!Array.isArray(events)||!events.length)return 0;
-  let cursor=Math.max(0,startCursor);
-  while(cursor<events.length&&events[cursor].date<open)cursor+=1;
-  const ordered=[];
-  for(let index=cursor;index<events.length;index+=1){const eventUnits=num(events[index]?.units);if(eventUnits>0)ordered.push(eventUnits);}
-  for(let index=0;index<Math.min(cursor,events.length);index+=1){const eventUnits=num(events[index]?.units);if(eventUnits>0)ordered.push(eventUnits);}
+  if(!unitsPerVial||unitsPerVial<=0||!Array.isArray(events)||!events.length)return 0;
+  const ordered=events.map(event=>num(event?.units)).filter(units=>units>0);
   if(!ordered.length)return 0;
   let remaining=unitsPerVial,count=0,index=0,safety=0;
   while(remaining>0.000001&&safety<100000){
@@ -12791,9 +12823,11 @@
 function cycleVialScheduleComputation(plan,cycleUsage,requestedTotal=null){
   if(!plan||plan.noCycle||!cycleUsage?.valid||cycleUsage.mode!=="cycle")return {valid:false,rows:[],required:0,reason:cycleUsage?.reason||"Complete the cycle dose and vial inputs to calculate the vial schedule."};
   const cycleNumber=Math.max(1,Math.floor(num(cycleUsage.cycleNumber)||1));
+  const requested=Math.max(0,Math.floor(num(requestedTotal)||0));
+  const renderCache=plannerRenderCalculationBucket(plan),renderCacheKey=`cycle-vial-schedule:${cycleNumber}:${requested}`;
+  if(renderCache?.has(renderCacheKey))return renderCache.get(renderCacheKey);
   const dosePlan=cycleVialDoseEvents(plan,cycleNumber);
   if(!dosePlan.valid)return {valid:false,rows:[],required:0,reason:dosePlan.reason};
-  const requested=Math.max(0,Math.floor(num(requestedTotal)||0));
   const cycleRecords=cycleVialScheduleRecordPool(plan,cycleUsage,cycleNumber),used=new Set();
   const rows=[];let cursor=0,previousFinish=localDateISO(dosePlan.period.start),required=0,index=1,previousPrep=cycleVialPrepForSequence(plan,cycleNumber);
   const unitsPerMl=Math.max(0,num(plan?.unitsPerMl)||0);
@@ -12857,7 +12891,9 @@
     if(!previousFinish&&needMoreAutomatic)break;
     index+=1;
   }
-  return {valid:true,rows,required,reason:"",dosePlan};
+  const result={valid:true,rows,required,reason:"",dosePlan};
+  if(renderCache)renderCache.set(renderCacheKey,result);
+  return result;
 }
 function cycleVialScheduleRows(plan,cycleUsage,requestedTotal=null){
   return cycleVialScheduleComputation(plan,cycleUsage,requestedTotal).rows;
@@ -13340,6 +13376,8 @@
 
 
 function continuousVialDoseEvents(plan){
+  const renderCache=plannerRenderCalculationBucket(plan),renderCacheKey="continuous-dose-events";
+  if(renderCache?.has(renderCacheKey))return renderCache.get(renderCacheKey);
   const start=parseISO(plan?.startDate);
   if(!start)return {valid:false,reason:"Enter the treatment start date"};
   const forecastDays=Math.max(7,automaticForecastWeeks(plan)*7),today=parseISO(todayISO());
@@ -13349,11 +13387,16 @@
   const simulation={...plan,enabled:true},events=[];
   for(let date=new Date(start);date<=horizonEnd;date=addDays(date,1)){
     const scheduled=scheduleForDate(simulation,date);if(!scheduled.administration)continue;
-    const doseMg=num(scheduled.doseMg);if(!(doseMg>0))continue;
-    events.push({date:new Date(date.getFullYear(),date.getMonth(),date.getDate()),dateISO:localDateISO(date),units:round(doseMg,6),doseMg,week:scheduled.week||Math.floor(dayDiff(start,date)/7)+1});
+    const administrations=(scheduled.administrations&&scheduled.administrations.length)?scheduled.administrations:[{partKey:"",partNumber:1,partCount:1,doseMg:scheduled.doseMg,units:scheduled.units,time:plan.injectionTime||""}];
+    for(const administration of administrations){
+      const doseMg=num(administration?.doseMg);if(!(doseMg>0))continue;
+      events.push({date:new Date(date.getFullYear(),date.getMonth(),date.getDate()),dateISO:localDateISO(date),units:round(doseMg,6),doseMg,week:scheduled.week||Math.floor(dayDiff(start,date)/7)+1,partKey:administration?.partKey||"",partNumber:administration?.partNumber||1,partCount:administration?.partCount||1,time:administration?.time||plan.injectionTime||""});
+    }
   }
   if(!events.length)return {valid:false,reason:"Complete the continuous schedule and dose"};
-  return {valid:true,start:new Date(start),horizonEnd:new Date(horizonEnd),events};
+  const result={valid:true,start:new Date(start),horizonEnd:new Date(horizonEnd),events};
+  if(renderCache)renderCache.set(renderCacheKey,result);
+  return result;
 }
 function continuousVialScheduleRecordPool(plan){
   return (state.vialRecords||[])
@@ -13371,6 +13414,8 @@
   if(!plan||!plan.noCycle)return {valid:false,rows:[],required:0,reason:"Continuous treatment required."};
   const dosePlan=continuousVialDoseEvents(plan);if(!dosePlan.valid)return {valid:false,rows:[],required:0,reason:dosePlan.reason};
   const requested=Math.max(0,Math.floor(num(requestedTotal??plan.continuousVialScheduleRowCount)||0));
+  const renderCache=plannerRenderCalculationBucket(plan),renderCacheKey=`continuous-vial-schedule:${requested}`;
+  if(renderCache?.has(renderCacheKey))return renderCache.get(renderCacheKey);
   const records=continuousVialScheduleRecordPool(plan),used=new Set(),rows=[];
   let cursor=0,previousFinish=localDateISO(dosePlan.start),required=0,index=1,previousPrep=null;
   const maxRows=Math.max(200,requested+20);
@@ -13402,7 +13447,9 @@
     const needMoreAutomatic=cursor<dosePlan.events.length,needMoreManual=index<requested;
     if(!needMoreAutomatic&&!needMoreManual)break;if(!previousFinish&&needMoreAutomatic)break;index+=1;
   }
-  return {valid:true,rows,required,reason:"",dosePlan};
+  const result={valid:true,rows,required,reason:"",dosePlan};
+  if(renderCache)renderCache.set(renderCacheKey,result);
+  return result;
 }
 function ensureContinuousVialScheduleRecord(plan,index){
   const computed=continuousVialScheduleComputation(plan),row=computed.rows.find(item=>item.index===Number(index));
@@ -15428,25 +15475,24 @@
   activateTab("dashboard");
 }
 function renderAll({preserveView=true}={}){
-  const view=preserveView?captureRenderView():null;
-  renderPlannerHeader();
-  reconcileAllPlanStartDates();
-  syncProfile();
-  renderDashboard();
-  renderProtocolLibrary();
-  renderSimplePlanner();
-  renderPlanCards();
-  renderGuidelinePlanOptions();
-  renderCalendar();
-  renderReferences();
-  renderAdminOptions();
-  renderTables();
-  renderSetupJourney();
-  renderGettingStartedJourney();
-  enhanceMobileTables();
-  enhanceDateInputs();
-  syncAllDateDisplays();
-  if(view)restoreRenderView(view);
+  const view=preserveView?captureRenderView():null,previousCycleConfigCache=plannerRenderCycleConfigCache,previousCalculationCache=plannerRenderCalculationCache;
+  plannerRenderCycleConfigCache=new WeakMap();
+  plannerRenderCalculationCache=new WeakMap();
+  try{
+    renderPlannerHeader();
+    reconcileAllPlanStartDates();
+    syncProfile();
+    const activeTabId=document.querySelector(".tab.active")?.id||"dashboard";
+    renderPrimaryTabContent(activeTabId);
+    renderPlanCards();
+    renderReferences();
+    renderSetupJourney();
+    renderGettingStartedJourney();
+    enhanceMobileTables();
+    enhanceDateInputs();
+    syncAllDateDisplays();
+    if(view)restoreRenderView(view);
+  }finally{plannerRenderCycleConfigCache=previousCycleConfigCache;plannerRenderCalculationCache=previousCalculationCache;}
 }
 let renderedDashboardDate=todayISO();
 setInterval(()=>{
--- a/peptide-planner-sw.js
+++ b/peptide-planner-sw.js
@@ -1,4 +1,4 @@
-const PEPTIDE_PLANNER_SW_VERSION='Peptide_Planner_v20261009.13';
+const PEPTIDE_PLANNER_SW_VERSION='Peptide_Planner_v20261109.01';
 self.addEventListener('install',event=>{self.skipWaiting();});
 self.addEventListener('activate',event=>{
   event.waitUntil((async()=>{
