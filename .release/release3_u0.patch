diff --git a/base/index.html b/new/index.html
index d41eecf..0f4bcd9 100644
--- a/base/index.html
+++ b/new/index.html
@@ -11 +11 @@
-<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
+<meta http-equiv="Cache-Control" content="no-cache, must-revalidate">
@@ -15 +15,2 @@
-<title>Peptide_Planner_v20261309.02.1</title>
+<title>Peptide_Planner_v20261309.03</title>
+<!-- v2026.1309.03: Data-resilience and runtime architecture release. Large per-user local-state writes are batched and flushed on page exit/sign-out; cross-device conflicts preserve an exportable local recovery copy before remote state can replace unsynced edits and refuse replacement if no recovery copy can be preserved; duplicate focus/visibility access checks are coalesced and throttled; HTTP/service-worker requests use revalidation rather than no-store so current deployments stay fresh while allowing conditional browser reuse. Treatment, dose, cycle, vial, date and calculation logic are unchanged from v2026.1309.02.1. -->
@@ -7230 +7231 @@ body.mobile-app #vialPurchaseReminderList .vial-purchase-check{
-        <div class="actions backup-transfer-actions"><button class="btn" id="exportBackup">Export backup</button><label class="btn" for="importBackup">Import backup</label><input id="importBackup" class="hidden" type="file" accept=".json,application/json"><button class="btn" id="printPlanner">Print / Save PDF</button><button class="btn danger" id="resetAll">Reset all data</button></div>
+        <div class="actions backup-transfer-actions"><button class="btn" id="exportBackup">Export backup</button><button class="btn hidden" id="exportConflictRecovery">Export sync recovery</button><label class="btn" for="importBackup">Import backup</label><input id="importBackup" class="hidden" type="file" accept=".json,application/json"><button class="btn" id="printPlanner">Print / Save PDF</button><button class="btn danger" id="resetAll">Reset all data</button></div>
@@ -7394 +7395 @@ const LEGACY_KEY="personalTreatmentTracker_v1";
-const APP_VERSION="Peptide_Planner_v20261309.02.1";
+const APP_VERSION="Peptide_Planner_v20261309.03";
@@ -7395,0 +7397,3 @@ const CLOUD_SYNC_META_KEY="peptidePlannerCloudSync_v1";
+const CLOUD_CONFLICT_RECOVERY_KEY="peptidePlannerConflictRecovery_v1";
+const LOCAL_STATE_SAVE_DELAY_MS=250;
+const RESUME_ACCESS_CHECK_MIN_MS=30000;
@@ -7403 +7407 @@ const PASSWORD_RESET_REDIRECT_URL=(()=>{
-let cloudClient=null,cloudUser=null,cloudAccess=null,cloudSaveTimer=null,cloudPushInFlight=null,cloudChangeRevision=0,cloudApplying=false,cloudInitialised=false,cloudAuthMode="signin",cloudAccessChecking=false;
+let cloudClient=null,cloudUser=null,cloudAccess=null,cloudSaveTimer=null,cloudPushInFlight=null,cloudChangeRevision=0,cloudApplying=false,cloudInitialised=false,cloudAuthMode="signin",cloudAccessChecking=false,plannerLocalSaveTimer=null,plannerLocalSaveUserId="",plannerResumeCheckTimer=null,lastPlannerAccessCheckAt=0;
@@ -9367,0 +9372,4 @@ function plannerUserSyncMetaKey(userId=cloudUser?.id){
+function plannerConflictRecoveryKey(userId=cloudUser?.id){
+  const id=String(userId||"").trim();
+  return id?`${CLOUD_CONFLICT_RECOVERY_KEY}:${id}`:"";
+}
@@ -9572,0 +9581,36 @@ function setCloudSyncMeta(patch,userId=cloudUser?.id){
+function persistPlannerStateNow(userId=plannerLocalSaveUserId||plannerStateUserId||cloudUser?.id){
+  const id=String(userId||"").trim();
+  if(!id||plannerStateUserId!==id)return false;
+  const key=plannerUserStorageKey(id);if(!key)return false;
+  try{localStorage.setItem(key,JSON.stringify(state));return true}catch(error){console.error("Planner storage unavailable",error);return false}
+}
+function schedulePlannerLocalSave(userId=cloudUser?.id){
+  const id=String(userId||"").trim();if(!id||plannerStateUserId!==id)return false;
+  plannerLocalSaveUserId=id;clearTimeout(plannerLocalSaveTimer);
+  plannerLocalSaveTimer=setTimeout(()=>{const target=plannerLocalSaveUserId;plannerLocalSaveTimer=null;plannerLocalSaveUserId="";persistPlannerStateNow(target)},LOCAL_STATE_SAVE_DELAY_MS);
+  return true;
+}
+function flushPlannerLocalSave(){
+  if(!plannerLocalSaveTimer&&!plannerLocalSaveUserId)return true;
+  clearTimeout(plannerLocalSaveTimer);plannerLocalSaveTimer=null;
+  const target=plannerLocalSaveUserId||plannerStateUserId;plannerLocalSaveUserId="";
+  return persistPlannerStateNow(target);
+}
+function cancelPlannerLocalSave(){clearTimeout(plannerLocalSaveTimer);plannerLocalSaveTimer=null;plannerLocalSaveUserId=""}
+function getConflictRecoverySnapshot(userId=cloudUser?.id){
+  const key=plannerConflictRecoveryKey(userId);if(!key)return null;
+  try{return JSON.parse(localStorage.getItem(key)||"null")}catch(_){return null}
+}
+function renderConflictRecoveryAction(){
+  const button=document.getElementById("exportConflictRecovery");if(!button)return;
+  const backup=getConflictRecoverySnapshot();button.classList.toggle("hidden",!backup?.state);
+  if(backup?.createdAt)button.title=`Unsynced local recovery saved ${fmtDateTime(backup.createdAt)}`;else button.removeAttribute("title");
+}
+function preserveLocalConflictRecovery(reason="cross_device_conflict_backup",userId=cloudUser?.id){
+  const id=String(userId||"").trim(),key=plannerConflictRecoveryKey(id);if(!id||!key||!hasMeaningfulPlannerData(state))return false;
+  try{
+    const meta=getCloudSyncMeta(id),createdAt=new Date().toISOString();
+    const backup={createdAt,reason,appVersion:APP_VERSION,localModifiedAt:meta.localModifiedAt||"",lastRemoteUpdatedAt:meta.lastRemoteUpdatedAt||"",state:clone(state)};
+    localStorage.setItem(key,JSON.stringify(backup));setCloudSyncMeta({conflictBackupAt:createdAt,conflictBackupReason:reason},id);renderConflictRecoveryAction();return true;
+  }catch(error){console.warn("Local conflict recovery could not be preserved",error);return false}
+}
@@ -9575,0 +9620 @@ function loadPlannerStateForUser(userId){
+  flushPlannerLocalSave();cancelPlannerLocalSave();
@@ -10042 +10087,11 @@ async function verifyPlannerAccess({silent=false,forceFirstUse=false}={}){
-  }finally{cloudAccessChecking=false}
+  }finally{lastPlannerAccessCheckAt=Date.now();cloudAccessChecking=false}
+}
+function schedulePlannerResumeCheck(){
+  if(!cloudUser||document.visibilityState==="hidden")return;
+  clearTimeout(plannerResumeCheckTimer);
+  plannerResumeCheckTimer=setTimeout(()=>{
+    plannerResumeCheckTimer=null;
+    if(!cloudUser||document.visibilityState==="hidden")return;
+    if(Date.now()-lastPlannerAccessCheckAt<RESUME_ACCESS_CHECK_MIN_MS)return;
+    verifyPlannerAccess({silent:true});
+  },250);
@@ -10478,0 +10534 @@ function renderCloudAccount(){
+  renderConflictRecoveryAction();
@@ -10490 +10546,4 @@ async function pullLatestCloudAfterRejectedPush(reason="cross_device_conflict_ba
-  if(getCloudSyncMeta(cloudUser.id).localDirty===true&&hasMeaningfulPlannerData(state))await snapshotLocalState(reason);
+  if(getCloudSyncMeta(cloudUser.id).localDirty===true&&hasMeaningfulPlannerData(state)){
+    const preserved=await snapshotLocalState(reason);
+    if(!preserved){setCloudSyncStatus("Sync conflict · recovery failed","error");return false}
+  }
@@ -10492 +10551 @@ async function pullLatestCloudAfterRejectedPush(reason="cross_device_conflict_ba
-  setCloudSyncStatus("Synced","synced");
+  setCloudSyncStatus(getConflictRecoverySnapshot()?"Synced · recovery saved":"Synced","synced");
@@ -10536,2 +10595,7 @@ async function snapshotLocalState(reason="local_device_backup"){
-  if(!cloudClient||!cloudUser||cloudAccess?.status!=="active"||!hasMeaningfulPlannerData(state))return;
-  try{await cloudClient.from("planner_snapshots").insert({user_id:cloudUser.id,state:state,app_version:APP_VERSION,reason})}catch(error){console.warn("Local pre-merge snapshot failed",error)}
+  if(!cloudUser||cloudAccess?.status!=="active"||!hasMeaningfulPlannerData(state))return true;
+  const localSaved=preserveLocalConflictRecovery(reason,cloudUser.id);
+  let cloudSaved=false;
+  if(cloudClient){
+    try{const {error}=await cloudClient.from("planner_snapshots").insert({user_id:cloudUser.id,state:state,app_version:APP_VERSION,reason});if(error)throw error;cloudSaved=true}catch(error){console.warn("Cloud pre-merge snapshot failed",error)}
+  }
+  return localSaved||cloudSaved;
@@ -10540,0 +10605 @@ function applyCloudPlannerState(remoteState,remoteStamp){
+  cancelPlannerLocalSave();
@@ -10574 +10639,2 @@ async function reconcileCloudState({silent=false}={}){
-        await snapshotLocalState(baseline?"cross_device_conflict_backup":"sync_baseline_backup");
+        const preserved=await snapshotLocalState(baseline?"cross_device_conflict_backup":"sync_baseline_backup");
+        if(!preserved){setCloudSyncStatus("Sync conflict · recovery failed","error");return}
@@ -10576 +10642 @@ async function reconcileCloudState({silent=false}={}){
-        setCloudSyncStatus("Synced","synced");
+        setCloudSyncStatus("Synced · recovery saved","synced");
@@ -10668 +10734 @@ async function signOutCloud(){
-  if(!cloudClient||!cloudUser)return;if(cloudAccess?.status==="active"){setCloudSyncStatus("Syncing…","syncing");await pushCloudState("sign_out")}
+  if(!cloudClient||!cloudUser)return;flushPlannerLocalSave();if(cloudAccess?.status==="active"){setCloudSyncStatus("Syncing…","syncing");await pushCloudState("sign_out")}
@@ -10777 +10843 @@ async function initCloudServices(){
-      cloudUser=null;cloudAccess=null;plannerStateUserId="";clearTimeout(cloudSaveTimer);cloudApplying=true;state=defaultState();normaliseState();cloudApplying=false;renderAll({preserveView:false});setPlannerLocked(true);renderPlannerAccessGate();renderCloudAccount();return
+      flushPlannerLocalSave();cancelPlannerLocalSave();cloudUser=null;cloudAccess=null;plannerStateUserId="";clearTimeout(cloudSaveTimer);cloudApplying=true;state=defaultState();normaliseState();cloudApplying=false;renderAll({preserveView:false});setPlannerLocked(true);renderPlannerAccessGate();renderCloudAccount();return
@@ -10786 +10852,3 @@ async function initCloudServices(){
-  window.addEventListener("focus",()=>{if(cloudUser)verifyPlannerAccess({silent:true})});document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"&&cloudUser)verifyPlannerAccess({silent:true})});
+  window.addEventListener("focus",schedulePlannerResumeCheck);
+  document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="hidden")flushPlannerLocalSave();else schedulePlannerResumeCheck()});
+  window.addEventListener("pagehide",flushPlannerLocalSave);
@@ -10791,2 +10859 @@ function save(){
-    const key=plannerUserStorageKey(cloudUser.id);if(!key)return false;
-    localStorage.setItem(key,JSON.stringify(state));markLocalChanged();scheduleCloudSave();return true;
+    markLocalChanged();schedulePlannerLocalSave(cloudUser.id);scheduleCloudSave();return true;
@@ -15730,0 +15798,6 @@ document.getElementById("exportBackup").onclick=()=>download(`peptide_treatment_
+document.getElementById("exportConflictRecovery").onclick=()=>{
+  const backup=getConflictRecoverySnapshot();
+  if(!backup?.state){renderConflictRecoveryAction();showAppMessage("No sync recovery","There is no preserved sync-conflict recovery copy for this account.","error");return}
+  const stamp=String(backup.createdAt||todayISO()).replace(/[:.]/g,"-");
+  download(`peptide_treatment_planner_sync_recovery_${stamp}.json`,JSON.stringify(backup.state,null,2),"application/json");
+};
@@ -15769,0 +15843 @@ document.getElementById("resetAll").onclick=()=>{
+    cancelPlannerLocalSave();
@@ -15770,0 +15845 @@ document.getElementById("resetAll").onclick=()=>{
+    const recoveryKey=plannerConflictRecoveryKey(cloudUser.id);if(recoveryKey)localStorage.removeItem(recoveryKey);
diff --git a/base/peptide-planner-sw.js b/new/peptide-planner-sw.js
index 54b20f7..950e266 100644
--- a/base/peptide-planner-sw.js
+++ b/new/peptide-planner-sw.js
@@ -1 +1 @@
-const PEPTIDE_PLANNER_SW_VERSION='Peptide_Planner_v20261309.02.1';
+const PEPTIDE_PLANNER_SW_VERSION='Peptide_Planner_v20261309.03';
@@ -20 +20 @@ self.addEventListener('fetch',event=>{
-  event.respondWith(fetch(event.request,{cache:'no-store'}).catch(()=>fetch(event.request)));
+  event.respondWith(fetch(event.request,{cache:'no-cache'}).catch(()=>fetch(event.request)));
diff --git a/base/_headers b/new/_headers
index a0275ab..4523d69 100644
--- a/base/_headers
+++ b/new/_headers
@@ -2 +2 @@
-  Cache-Control: no-store, no-cache, must-revalidate, max-age=0
+  Cache-Control: no-cache, must-revalidate
