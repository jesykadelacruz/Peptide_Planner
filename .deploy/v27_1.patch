--- a/index.html
+++ b/index.html
@@ -8,7 +8,7 @@
 <meta name="apple-mobile-web-app-status-bar-style" content="default">
 <meta name="apple-mobile-web-app-title" content="Peptide Planner">
 <meta name="mobile-web-app-capable" content="yes">
-<title>Peptide Planner v27</title>
+<title>Peptide Planner v27.1</title>
 <!-- stabilization build: backend signup, verified Today, dose/units authority, consolidated treatment CSS, direct single-file deployment -->
 <style id="production-css">
 /* ===== consolidated source: base ===== */
@@ -4789,6 +4789,22 @@
 .verification-missing-list{margin:6px 0 0;padding-left:18px}
 .verification-missing-list li{margin:2px 0}
 
+
+/* Verification checkbox interaction only: standard checkbox size/alignment; no other field sizing changes. */
+.review-verify-block .confirmation{
+  cursor:pointer;
+}
+.review-verify-block .confirmation input[type="checkbox"]{
+  width:18px!important;
+  height:18px!important;
+  min-width:18px!important;
+  min-height:18px!important;
+  flex:0 0 18px!important;
+  margin:1px 0 0!important;
+  padding:0!important;
+  cursor:pointer!important;
+  accent-color:var(--green);
+}
 </style>
 
 
@@ -4953,7 +4969,7 @@
           <div class="payment-proof-box">
             <label>Payment screenshot</label>
             <button id="plannerPaymentScreenshotButton" class="payment-upload-trigger" type="button">Upload Payment Screenshot</button>
-            <input id="plannerPaymentScreenshot" class="payment-upload-file" type="file" accept="image/*" required>
+            <input id="plannerPaymentScreenshot" class="payment-upload-file" type="file" accept="image/jpeg,image/png,image/webp" required>
             <div id="plannerPaymentScreenshotState" class="payment-upload-state">No screenshot uploaded.</div>
             <div class="payment-proof-required">Required before payment can be submitted for verification.</div>
             <div id="plannerPaymentScreenshotPreview" class="payment-proof-preview">
@@ -6163,7 +6179,7 @@
 "use strict";
 const STORAGE_KEY="peptideTreatmentPlanner_v4";
 const LEGACY_KEY="personalTreatmentTracker_v1";
-const APP_VERSION="v27";
+const APP_VERSION="v27.1";
 const CLOUD_SYNC_META_KEY="peptidePlannerCloudSync_v1";
 const CLOUD_KB_CACHE_KEY="peptidePlannerKbOverrides_v1";
 const SUPABASE_URL="https://srfuwxjwulvbalzoymcc.supabase.co";
@@ -8064,12 +8080,12 @@
   reconcileCycleStackStartDates();
   state.plans.forEach(reconcilePlanStackStatus);
   state.plans.forEach(initialisePlanPhaseEntryModes);
-  state.plans.forEach(plan=>{if(plan.confirmed&&!planVerificationCompleteness(plan).valid)plan.confirmed=false});
+  /* User verification is retained; Today readiness remains gated by planReadyForToday(plan). */
   if(!onboardingFlagPresent){
     const p=state.profile||{};
     const profileComplete=!!(String(p.name||"").trim()&&p.dob&&num(p.heightCm)>0&&num(p.startWeight)>0&&num(p.targetWeight)>0);
     const personalPlans=(state.plans||[]).filter(plan=>["active","soon","completed"].includes(plan.stackStatus));
-    const hasVerified=personalPlans.some(plan=>!!plan.confirmed);
+    const hasVerified=personalPlans.some(plan=>planReadyForToday(plan));
     state.ui.onboardingStage=profileComplete?(personalPlans.length?(hasVerified?"complete":"verify"):"treatment"):"profile";
     state.ui.onboardingComplete=profileComplete&&hasVerified;
   }else{
@@ -11168,17 +11184,13 @@
       else{
         const recalculatesDose=["vialMg","bacWaterMl","finalMl","unitsPerMl"].includes(field);
         if(recalculatesDose)initialisePlanPhaseEntryModes(plan);
-        if(field==="confirmed"&&control.checked){
-          const verificationCheck=planVerificationCompleteness(plan);
-          if(!verificationCheck.valid){plan.confirmed=false;control.checked=false;save();renderAll();return}
-        }
         plan[field]=control.type==="checkbox"?control.checked:control.value;
         if(field==="startDate"&&isSeparateCycleStack(plan))plan.cycleStartAuto=false;
         if(!["confirmed","enabled","stackStatus"].includes(field))plan.confirmed=false;
         if(field==="intervalDays"&&Number(control.value)>0){plan.scheduleMode="interval";plan.weekdays=[];}
         if(field==="bacWaterMl")plan.finalMl=control.value;
         if(recalculatesDose)syncPlanPhaseCalculations(plan);
-        if(field==="confirmed"&&control.checked&&plannerOnboardingStage()==="verify"){
+        if(field==="confirmed"&&control.checked&&planReadyForToday(plan)&&plannerOnboardingStage()==="verify"){
           state.ui.onboardingStage="tour";state.ui.onboardingComplete=false;showTourAfter=true;
         }
         save();
@@ -12551,7 +12563,7 @@
   requestAnimationFrame(()=>{
     const card=[...document.querySelectorAll("[data-simple-plan]")].find(item=>{
       const plan=state.plans.find(plan=>plan.id===item.dataset.simplePlan);
-      return plan&&!plan.confirmed&&["active","soon"].includes(plan.stackStatus);
+      return plan&&!planReadyForToday(plan)&&["active","soon"].includes(plan.stackStatus);
     })||document.querySelector("[data-simple-plan]");
     if(card){card.open=true;card.scrollIntoView({behavior:"smooth",block:"start"});setTimeout(()=>card.querySelector(".review-verify-block")?.scrollIntoView({behavior:"smooth",block:"center"}),180)}
   });
@@ -12606,7 +12618,7 @@
   const profileDone=plannerProfileComplete();
   const plans=plannerTreatmentPlans();
   const treatmentDone=plans.length>0;
-  const verified=plans.some(plan=>!!plan.confirmed);
+  const verified=plans.some(plan=>planReadyForToday(plan));
   const ready=verified;
   const set=(id,status,done,current)=>{const button=document.getElementById(id),small=document.getElementById(id+"Status");if(button){button.classList.toggle("complete",!!done);button.classList.toggle("current",!!current)}if(small)small.textContent=status};
   const stage=plannerOnboardingStage();
