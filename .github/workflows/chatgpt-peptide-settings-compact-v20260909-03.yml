name: Temporary cycled vial preparation inspection

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/chatgpt-peptide-settings-compact-v20260909-03.yml'

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Print targeted treatment functions
        run: |
          python - <<'PY'
          from pathlib import Path
          s=Path('index.html').read_text(encoding='utf-8')
          def block(start, end):
              a=s.index(start)
              b=s.index(end,a)
              print('\n===== '+start+' =====\n')
              print(s[a:b])
          block('function continuousVialPrepForRecord', 'function continuousVialPrepForDate')
          block('function continuousVialPrepForDate', 'function continuousVialPrepForAdministration')
          block('function calcUnits', 'function simplePhaseSplitParts')
          block('function cycleVialDoseEvents', 'function cycleVialUsage')
          block('function cycleVialUsage', 'function cycleVialScheduleStatus')
          block('function cycleVialScheduleComputation', 'function ensureCycleVialScheduleRecord')
          needle='missing.push("vial strength")'
          p=s.index(needle)
          a=s.rfind('\nfunction ',0,p)+1
          b=s.find('\nfunction ',p)
          print('\n===== verification function =====\n')
          print(s[a:b])
          needle='card.querySelectorAll("[data-cycle-vial-field]")'
          p=s.index(needle)
          a=s.rfind('\n',0,p)+1
          b=s.find('    card.querySelectorAll("[data-cycle-vial-open-date]")',p)
          print('\n===== cycled vial change handler =====\n')
          print(s[a:b])
          PY
